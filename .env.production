# Production environment variables
# Copy this to .env and fill in real values

# PostgreSQL password (generate strong random string)
POSTGRES_PASSWORD=CHANGE_ME_generate_random_32_chars

# Flask secret key (generate strong random string)
SECRET_KEY=CHANGE_ME_generate_random_64_chars

# Domain name (update after buying domain)
DOMAIN=your-domain.com
